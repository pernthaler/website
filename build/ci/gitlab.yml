stages:
  - client
  - build
  - docker

build:
  stage: build
  image: golang:latest
  script:
    - apt-get update
    - apt-get install upx-ucl
    - make

docker:
  stage: docker
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker restart Pernthaler