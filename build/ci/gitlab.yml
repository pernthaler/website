stages:
  - client
  - build
  - docker

build:
  stage: build
  image: golang:latest
  script:
    - make

docker:
  stage: docker
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker restart Pernthaler